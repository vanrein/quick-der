DESTDIR ?=
PREFIX ?= /usr/local
DESTPATH=$(abspath $(DESTDIR)/$(PREFIX))

all:

clean:

install:
	install asn2quickder.py $(DESTPATH)/bin/asn2quickder

uninstall:
	rm -f $(DESTPATH)/bin/asn2quickder

